# Dev/test dependencies for Telearr.
# Install into a venv with access to the runtime deps (bcrypt, etc.):
#   python -m venv --system-site-packages .venv && .venv/bin/pip install -r requirements-dev.txt
pytest>=9.1.1,<10.0
pytest-asyncio>=1.4.0,<1.5   # for future async API tests
httpx>=0.27,<0.29            # ASGI test client for future FastAPI tests
ruff>=0.16.2,<0.17              # linter (config in pyproject.toml)
